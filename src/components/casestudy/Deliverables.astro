---
interface Props { items?: string[]; }
const { items = [] } = Astro.props;
---
{items.length > 0 && (
  <div class="cs-block">
    <h3 class="cs-block-label">Deliverables</h3>
    <ul class="cs-deliverables-list">
      {items.map((item) => <li>{item}</li>)}
    </ul>
  </div>
)}
